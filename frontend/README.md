# Look at the github 

# Action types
- Action type files
  
enum file that defines 

- Switch statement and cases
- 
- Reducer: Look at what you pass to it and dispatch (fancy useEffect that can do multiple things based on action type)
- call the api with header as the api token
  - Try: tries to call 
  - Catch: catches error, server refuses the request or any other error
- JWT stored in local storage
- dispatch type is the 

in state:
- have a store js file to get everything into one root reducer

Storing the JWT in local storage:
- Assumes that the user goes through the authentication. If the user is authenticated, the JWT is stored in local storage.
- If the user is not authenticated, the JWT is not stored, and the user will be prompted to log in again to obtain a new token.
- How to store the 